.header
  %h1 Teeworlds Ladder

.content
  %ul
    - @players[:players].each do |player|
      %li
        %span= player.name
        %span= player.clan
        %span= player.rating.to_s

!= paginate @players, items_per_page: players_per_page, labels: {first: '«', last: '»'}, renderer: 'haml'
