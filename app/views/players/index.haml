%table.ui.basic.table
  %thead
    %tr
      %th Rank
      %th Nickname
      %th Clan
      %th Rating
    - @players.each_index do |i|
      %tr
        %td= ((page - 1) * Player.per_page) + i + 1
        %td= link_to @players[i].name, player_path(@players[i].name)
        %td= link_to @players[i].clan, clan_path(@players[i].clan) unless @players[i].clan.blank?
        %td= @players[i].rating.to_s
= will_paginate @players, :class => '', :id => 'pagination'
